name: wifiot
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  wifi_iot: ^0.3.19
  plugin_wifi_connect:
    git:
      url: https://github.com/tadodotcom/plugin_wifi_connect
  wifi_scan: ^0.4.1
  android_flutter_wifi: ^0.1.2
  webview_flutter: ^4.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true


